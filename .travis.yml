language: cpp
dist: trusty
sudo: required
python: '3.6'
compiler:
 - gcc
script:
 - mkdir build && cd build && cmake .. -DUSE_SPGLIB=ON -DBUILD_QC=OFF -DUSE_SYSTEM_EIGEN3=OFF -DBUILD_PYTHON_BINDINGS=ON && cmake --build .
 
